name: trial-build

on:
  workflow_dispatch: {}

jobs:
  build:
    runs-on: macos-26
    timeout-minutes: 180
    permissions:
      contents: write
    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Disk usage before cleanup
        run: df -h /

      - name: Free disk space (remove unused Xcode versions, simulators, junk)
        run: |
          set -x
          sudo rm -rf /Applications/Xcode_16*.app /Applications/Xcode_17*.app 2>/dev/null || true
          sudo rm -rf ~/Library/Developer/CoreSimulator/Caches
          sudo rm -rf ~/Library/Developer/Xcode/DerivedData
          sudo rm -rf /Library/Developer/CoreSimulator/Profiles/Runtimes/*.simruntime 2>/dev/null || true
          df -h /

      - name: Select Xcode 26.2
        run: |
          sudo xcode-select -s /Applications/Xcode_26.2.app || \
          sudo xcode-select -s /Applications/Xcode.app
          xcodebuild -version

      - name: Write minimal development configuration
        env:
          TG_API_ID: ${{ secrets.TG_API_ID }}
          TG_API_HASH: ${{ secrets.TG_API_HASH }}
        run: |
          python3 - <<'EOF'
          import json, os, secrets
          path = "build-system/template_minimal_development_configuration.json"
          with open(path) as f:
              cfg = json.load(f)
          cfg["bundle_id"] = f"org.{secrets.token_hex(8)}.Swiftgram"
          cfg["api_id"] = os.environ["TG_API_ID"]
          cfg["api_hash"] = os.environ["TG_API_HASH"]
          cfg["team_id"] = "0000000000"  # dummy: simulator build below disables real code signing
          with open("build-system/generated_trial_configuration.json", "w") as f:
              json.dump(cfg, f, indent=2)
          print(json.dumps(cfg, indent=2))
          EOF

      - name: Generate Xcode project via Bazel
        run: |
          python3 build-system/Make/Make.py \
            --cacheDir="$HOME/telegram-bazel-cache" \
            generateProject \
            --configurationPath=build-system/generated_trial_configuration.json \
            --xcodeManagedCodesigning

      - name: Disk usage after project generation
        run: df -h /

      - name: Locate generated .xcodeproj
        id: proj
        run: |
          set -euo pipefail
          PROJ_PATH=$(find . -maxdepth 3 -iname "*.xcodeproj" -not -path "*/bazel-*" | head -1)
          if [ -z "$PROJ_PATH" ]; then
            echo "No .xcodeproj found after generateProject" >&2
            find . -maxdepth 3 -iname "*.xcodeproj" -o -iname "*.xcworkspace"
            exit 1
          fi
          echo "Found project: $PROJ_PATH"
          echo "path=$PROJ_PATH" >> "$GITHUB_OUTPUT"

      - name: List available schemes
        id: scheme
        run: |
          set -o pipefail
          xcodebuild -list -project "${{ steps.proj.outputs.path }}" | tee scheme_list.txt
          SCHEME=$(awk '/Schemes:/{flag=1; next} flag && NF{print $1; exit}' scheme_list.txt)
          echo "Using scheme: $SCHEME"
          echo "name=$SCHEME" >> "$GITHUB_OUTPUT"

      - name: Build for simulator (no code signing, just validating the pipeline)
        run: |
          set -o pipefail
          xcodebuild build \
            -project "${{ steps.proj.outputs.path }}" \
            -scheme "${{ steps.scheme.outputs.name }}" \
            -sdk iphonesimulator \
            -configuration Debug \
            -derivedDataPath "$HOME/derived-sim" \
            CODE_SIGNING_ALLOWED=NO \
            | tee build.log

      - name: Disk usage after build
        if: always()
        run: df -h /

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trial-build-log
          path: |
            build.log
            scheme_list.txt

      - name: Build for device (unsigned)
        run: |
          set -o pipefail
          xcodebuild build \
            -project "${{ steps.proj.outputs.path }}" \
            -scheme "${{ steps.scheme.outputs.name }}" \
            -sdk iphoneos \
            -configuration Debug \
            -derivedDataPath "$HOME/derived-device" \
            -destination "generic/platform=iOS" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            DEVELOPMENT_TEAM="" \
            | tee build-device.log

      - name: Package unsigned IPA
        id: ipa
        run: |
          set -euo pipefail
          PRODUCTS_DIR=$(find "$HOME/derived-device/Build/Products" -maxdepth 1 -iname "*-iphoneos" | head -1)
          APP_PATH=$(find "$PRODUCTS_DIR" -maxdepth 1 -iname "*.app" | head -1)
          if [ -z "$APP_PATH" ]; then
            echo "No .app found in $PRODUCTS_DIR" >&2
            exit 1
          fi
          echo "Found app: $APP_PATH"
          SHORT_SHA=$(git rev-parse --short HEAD)
          IPA_NAME="ViboGram-unsigned-${SHORT_SHA}.ipa"
          PACK_DIR=$(mktemp -d)/Payload
          mkdir -p "$PACK_DIR"
          cp -R "$APP_PATH" "$PACK_DIR/"
          (cd "$(dirname "$PACK_DIR")" && zip -r -y -q "$GITHUB_WORKSPACE/$IPA_NAME" Payload)
          echo "path=$IPA_NAME" >> "$GITHUB_OUTPUT"
          ls -lh "$IPA_NAME"

      - name: Upload device build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trial-build-device-log
          path: build-device.log

      - name: Publish unsigned IPA as a versioned pre-release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          TAG="v0.0.${{ github.run_number }}"
          gh release create "$TAG" "${{ steps.ipa.outputs.path }}" \
            --title "$TAG ($(git rev-parse --short HEAD))" \
            --notes "Unsigned device IPA, auto-published from trial-build.yml. Not signed — needs resigning (LiveContainer/AltStore/SideStore/ldid) before install. Development build, not v1.0.0 yet." \
            --prerelease
